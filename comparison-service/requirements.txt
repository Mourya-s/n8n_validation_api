fastapi
uvicorn[standard]
pydantic
pyodbc

# Databricks SQL Warehouse connectivity - required by databricks_connector.py
# (was commented out, but the module already does `from databricks import sql`)
databricks-sql-connector

# Used by comparison_engine.py (already imported, was missing here)
pandas
numpy

# Azure Blob Storage connectivity - required by azure_connector.py
azure-storage-blob

# Used by report_generator.py for the Excel validation report, and by
# azure_connector.py for reading Excel-format blobs
openpyxl

# Used by azure_connector.py for reading Parquet-format blobs
pyarrow

# Testing
pytest