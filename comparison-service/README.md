# Comparison Service

Minimal FastAPI-based scaffold for comparing items from Azure SQL / Databricks.

Run locally:

```bash
pip install -r requirements.txt
uvicorn app:app --reload
```

Test with:

```bash
python test_api.py
```
